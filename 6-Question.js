// Stripping Names: Store a person's name, and include some whitespace charachters at the begining and end of the name.
//Make sure you use each cheracter combination, "\t" and "\n", at least once. print the name once,
//so the whitespace around the name is displayed. Then print the name after strping the white spaces
let nameSpace = "\tMuhammad \n\tSalman";
console.log(nameSpace);
export {};
